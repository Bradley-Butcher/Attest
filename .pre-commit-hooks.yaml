- id: attest
  name: Attest commit signoff
  description: Require evidence-backed Attest contract signoff before commit.
  entry: attest pre-commit-hook
  language: rust
  pass_filenames: false
  always_run: true
  require_serial: true
  minimum_pre_commit_version: "3.2.0"
  stages: [pre-commit]
